<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.oods.thelast">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <!--<service-->
            <!--android:name=".MyService"-->
            <!--android:enabled="false"-->
            <!--android:exported="false"/>-->

        <activity android:name=".component.ListData" />
        <activity android:name=".component.AdminActivity" />
        <activity android:name=".component.PilihGedung" />
        <activity android:name=".component.DetailJasa" />
        <activity
            android:name=".component.AddJasa"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="Add Jasa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".component.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".component.Splashscreen">

            <!-- <activity android:name=".Crud.Satu"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>